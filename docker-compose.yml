services:
  web:
    build:
      context: ./apps/web
      dockerfile: Dockerfile.dev
    ports:
      - "3050:3050"
    volumes:
      - ./apps/web/src:/app/src
      - ./apps/web/public:/app/public
      - ./apps/web/constants:/app/constants
      - ./apps/web/index.html:/app/index.html
    environment:
      - NODE_ENV=development
